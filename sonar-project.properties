# Unique key for your project (choose any, must match your SonarCloud project)
sonar.projectKey=my-vulnerable-java

# Your real SonarCloud organization key (from SonarCloud dashboard → My Organizations)
sonar.organization=tnumber75-sudo

# SonarCloud server URL
sonar.host.url=https://sonarcloud.io

# Path to your source code
sonar.sources=src

# Path to compiled .class files (Maven default is target/classes)
sonar.java.binaries=target/classes
